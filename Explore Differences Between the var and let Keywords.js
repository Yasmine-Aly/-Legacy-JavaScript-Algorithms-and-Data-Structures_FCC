let catName = "Oliver";
let catSound = "Meow!";
/* 
Use let when you need block-scoped variables, want to avoid issues with hoisting, or want to prevent accidental redeclaration.
Use var if you need function-scoped variables, but in modern JavaScript, let (or const for immutable bindings) is generally preferred.
*/
